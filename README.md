# Bitland Empire

Small teaching tools, kept as independent apps under `tools/`.

## Tools

- `tools/cpu-voyeur` - clickable MIPS64 datapath and instruction execution demo.

Each tool owns its own dependencies and scripts so more tools can be added without turning the repository into a heavy framework.
